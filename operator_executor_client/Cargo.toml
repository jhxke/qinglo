[package]
name = "operator_executor_client"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[lib]
crate-type = ["rlib"]

[dependencies]
operator_runtime = { path = "../operator_runtime" }
operator_sdk = { path = "../operator_sdk" }
serde_json = "1.0"
thiserror = "1.0"